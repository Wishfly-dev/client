name: client
description: Wishfly client.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.18.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: 0.13.3
  provider: 6.0.5
  shared_preferences: 2.1.0
  shared:
    git: https://github.com/Wishfly-dev/shared.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  assets:
    - assets/images/
    - assets/l10n/
